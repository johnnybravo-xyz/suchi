---
title: "Mail intake"
description: "Connect Outlook, Gmail, Fastmail, iCloud, Zoho, Proton Bridge, or another IMAP mailbox."
---

Suchi can poll multiple IMAP mailboxes. Each message is stored as an EML
document, attachments become child documents, and normal extraction, filing,
and automation behavior follows.

Each mailbox has its own owner, credentials, folder, poll interval, filters,
cursor, and health state. A failing mailbox does not stop other pollers.

## Provider setup

| Provider | Incoming server | Authentication |
| --- | --- | --- |
| Microsoft / Outlook | `outlook.office365.com:993` | Suchi's Microsoft device-code sign-in |
| Gmail | `imap.gmail.com:993` | Google app password |
| Fastmail | `imap.fastmail.com:993` | Fastmail app password |
| iCloud Mail | `imap.mail.me.com:993` | Apple app-specific password |
| Zoho Mail | `imap.zoho.com:993` | Password or application-specific password |
| Proton Mail | Local Proton Bridge | Bridge-generated IMAP credentials |
| Custom | Provider supplied | Password or app password |

Provider presets fill host, port, TLS, and default authentication. They do not
bypass provider account policy.

### Microsoft and Outlook

Pick **Microsoft** and click **Sign in with Microsoft**. Published builds use
Suchi's public-client registration, so ordinary users need no Azure account,
client ID, or secret. A deployment operator may set a different public-client
ID in the config file or environment before starting Suchi.

See [Microsoft mailbox sign-in](/guides/mail-intake-oauth).

### Gmail

v0.1 uses an app password, not the normal Google Account password or Google
OAuth:

1. Enable [2-Step Verification](https://support.google.com/accounts/answer/185839).
2. Create a value under [Google App Passwords](https://myaccount.google.com/apppasswords).
3. Use the full Gmail address and the generated 16-character password.

Workspace policy, Advanced Protection, or security-key-only verification may
disable app passwords. Suchi cannot connect those Gmail accounts in v0.1.

### iCloud Mail

v0.1 uses an app-specific password, not the normal Apple Account password:

1. Turn on two-factor authentication for the Apple Account.
2. Open [Apple Account](https://account.apple.com/) and create an app-specific
   password for Suchi under **Sign-In and Security > App-Specific Passwords**.
3. Use the part before `@icloud.com` as the username and paste the generated
   password into Suchi.

If that username is rejected, try the full iCloud Mail address. Changing the
Apple Account password revokes all app-specific passwords. See
[Apple's app-specific password instructions](https://support.apple.com/102654).

### Fastmail and Zoho

- Fastmail: create an app password under **Settings > Privacy & Security >
  Integrations**. The account plan must include IMAP.
- Zoho: enable IMAP and use an application-specific password when account
  policy requires it. Some organizations and regions use a host other than
  `imap.zoho.com`; copy the server shown in Zoho settings.

### Proton Bridge and custom IMAP

Proton Mail requires Bridge on a trusted machine or container network. Use the
Bridge-generated username and password, and configure its local host, port,
TLS, and CA settings in Suchi.

For Yahoo, Dovecot, mailcow, or another provider, choose **Custom** and use its
incoming IMAP values. Prefer TLS on port 993 and a provider-scoped app password.
Suchi reads mail only and needs no SMTP configuration.

## Add a mailbox

1. Open **Settings > Mailboxes** and select **Add mailbox**.
2. Choose the provider, owner, display name, and mailbox username.
3. Enter an app password or complete Microsoft sign-in.
4. Select the folder and poll interval.
5. Optionally choose a processed folder or enable marking messages as read.
6. Set **Sync mail from**. New accounts default to the current time so adding a
   mailbox does not import years of history unexpectedly.
7. Optionally restrict senders or require attachments.
8. Save, then run **Test connection**.

The supervisor starts or reloads the mailbox within seconds. Adding, editing,
disabling, deleting, testing, signing in, or changing mailbox credentials does
not require a Suchi restart.

Suchi selects authentication from the provider: Microsoft always uses OAuth;
the other presets use their app-specific or Bridge password. Custom IMAP uses
password authentication. There is no separate authentication-mode switch.

## History and cursor behavior

`sync_since` is an absolute initial-history floor expressed as Unix seconds.
Set it to an earlier time to backfill or to zero to allow all history. The
durable UID cursor still prevents already processed messages from being read
again.

If a message fails, the cursor does not advance past it. Deduplication uses
`Message-ID` per owner, including messages that also arrive through a watched
folder or mbsync.

Deleting or moving a message in the provider while Suchi is polling is harmless.
Suchi skips a message that disappears between mailbox discovery and download;
it does not mark the mailbox unhealthy or remove an already archived document.

Without a processed folder, Suchi leaves server read state unchanged unless
**Mark messages as read** is enabled.

## Filters and automations

The sender allowlist accepts full addresses or `@domain` suffixes. Empty means
all senders. **Attachments only** drops messages without attachments before
creating a document.

Consumption automations can filter on sender, subject, source folder, and
attachment presence. Use them to assign correspondents, tags, document types,
filing categories, or storage paths. See [Rules and automations](/automations).

## Alternative mbsync path

Use [mbsync](/guides/mail-mbsync) when you need a local Maildir mirror, already
run mbsync, or want Suchi to receive files without storing mailbox credentials.
The built-in poller is the simpler default.

## Troubleshooting

- **Microsoft sign-in fails:** confirm IMAP access is enabled for the mailbox,
  then retry **Sign in with Microsoft** and review any tenant consent policy.
- **Microsoft sign-in unavailable:** remove an invalid configured client ID to
  restore Suchi's shipped registration on the next start.
- **Gmail requests an application password:** enable 2-Step Verification and
  use a Google app password. There is no Google OAuth fallback in v0.1.
- **Provider rejects credentials:** verify that the credential is an app
  password and the plan or organization allows IMAP.
- **Messages missing:** inspect `sync_since`, sender allowlist, attachments-only
  mode, source folder, and the mailbox row's last error.
- **Changes seem ignored:** wait a few seconds for supervisor reconciliation,
  then inspect `emailwatch.supervisor` logs. Do not restart Suchi.

Provider references: [Google IMAP](https://support.google.com/mail/answer/7126229),
[Fastmail IMAP](https://www.fastmail.help/hc/en-us/articles/1500000279921),
[Apple Mail settings](https://support.apple.com/102525),
[Zoho IMAP](https://www.zoho.com/mail/help/imap-access.html), and
[Proton Bridge](https://proton.me/support/imap-smtp-and-pop3-setup).
