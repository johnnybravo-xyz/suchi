# English strings for the server-rendered authentication pages.
# The main application is the Svelte SPA; keep this catalog limited to
# templates under core/ui/templates.

auth.login.title: Sign in
auth.login.subtitle: to continue to suchi.
auth.login.email: Email
auth.login.password: Password
auth.login.submit: Sign in

auth.setup.title: Set up suchi
auth.setup.body: First-boot admin. The one-time token is in your server log as
auth.setup.body_suffix: Paste it below.
auth.setup.token: Setup token
auth.setup.token_placeholder: paste the token from the log
auth.setup.email: Admin email
auth.setup.display_name: Display name
auth.setup.optional: (optional)
auth.setup.password: Password
auth.setup.submit: Create admin
