The frontend onboarding doc lives in the main docs tree so it
renders next to the rest of the developer reference. See
`docs/spa-architecture.mdx` (or the rendered version at
`/spa-architecture` on the docs site). If you're editing this
tree, read it first — it covers the constraints, the build
pipeline, routing, state, the API layer, and the design system in
one place.
